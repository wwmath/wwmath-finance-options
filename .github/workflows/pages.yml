# Publishes the WWMath Time Machine to GitHub Pages:
#   https://wwmath.github.io/wwmath-finance-options/
# Only the single-file page is deployed, not the repo (so the PDFs are not re-served).
# One-time setup: Settings -> Pages -> Build and deployment -> Source: GitHub Actions.
name: Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # rebuild so the page's GitHub links point at the branch being published
      - run: python timeline/build.py --ref "${{ github.ref_name }}"
      - run: |
          mkdir -p site
          cp timeline/wwmath-time-machine.html site/index.html
          touch site/.nojekyll
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: site
      - id: deployment
        uses: actions/deploy-pages@v4
